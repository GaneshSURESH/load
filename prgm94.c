#include<stdio.h>
#include<conio.h>
int main()
{
    int a,b,c;
    Scanner s=new Scanner(System.in);
    a=s.nextInt();
    b=s.nextInt();
    for(int i=1;i<=a;i++){
    if(b==i)
        {
            printf(i);
        }
    }

           
   }
}

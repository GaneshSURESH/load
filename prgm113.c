#include <stdio.h>
#include<conio.h>
int main()
{
   int a[10],s,k,count=0,i;
   clrscr();
   scanf("%d %d",&s,&k);
   for(i=0;i<s0;i++)
   {
   scanf("%d",&a[i]);
   }
   for(i=0;i<s;i++)
   {
       if(a[i]==k)
       count++;
   }
   printf("%d",count);
    return 0;
}
